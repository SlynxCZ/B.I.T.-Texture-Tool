B.I.T. Texture Tool V0.17.16

Rebuilt directly from the trusted V0.17.14 source branch (not from V0.17.15).

UI fixes:
- Reworked Autonomous speed layout for every language.
- "Autonomous speed" now has its own line instead of consuming horizontal room beside the radio buttons.
- Speed modes use a stable 3-column grid: Slow / Normal / Fast, then Extreme / Custom.
- Longer German, Portuguese, Russian, Czech, etc. labels get the full column width and no longer drift or collide.
- Settings > Custom workers now matches Game / Overwrite mode: muted section label above a full-width selector.
- Custom workers label now uses the same Settings color/style as the other category labels.
- Footer is a compact single line: V0.17.16 • Made by Tabo using ChatGPT.

V0.17.14 functionality retained:
- Hammer-safe replacement/rollback behavior.
- Custom 1-32 Autonomous workers.
- Adaptive Slow/Normal/Fast/Extreme worker profiles.
- Retry, compiler locking, Ask/Skip/Replace overwrite modes, translations and CS2 workflow.
